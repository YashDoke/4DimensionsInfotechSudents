#include<stdio.h>

// finding area and perimeter of rectangle
int main(){
    int l=5, b=7;
    printf("Area of Rectangle is: %d", (l*b));
    printf("\nPerimeter: %d",2*(l+b));
    return 0;
}