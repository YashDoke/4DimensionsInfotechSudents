#include<stdio.h>
int main()
{
    float radius=4;
    int length,width;
    scanf("%f",&radius);
    float pi=3.14;
    float   perimeter;
      perimeter= 2*pi*radius;
    
   //printf("area of circle is:%f",area);
    printf("perimeter of circle is:%f",perimeter);

    return 0;

}