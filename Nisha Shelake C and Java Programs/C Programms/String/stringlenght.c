#include<stdio.h>
#include<string.h>
void main()
{
    int count;
    char str[20];
    printf("enter a string\n");
    scanf("%s",&str);
    int i=0;
    for(i=0;str[i]!='\0';i++)
    {
        count=i;
    }
    printf("length of string is %d",count);
}`